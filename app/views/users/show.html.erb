

  <div class = "container">
    <br>
    <div class = "jumbotron">
    <% if @user.hats.any? %>
      <%  if @user.id == session[:id] %>
        <h1 class = "display-4">Hello, <%= @user.username %>!</h1><br><br>
        <h2>Here are your hats!</h2>
      <% else %>
        <h1 class = "display-4">Welcome to <%= @user.username %>'s page!</h1><br><br>
        <h2>Check out <%= @user.username %>'s hats!
      <% end %>
        <% @user.hats.each do |hat| %>
          <% if hat.hat_pics.any? %>
            <%= link_to(image_tag(hat.hat_pics.all.sample.avatar.url(:medium)), hat_path(hat)) %>
          <% else %>
            <h3><%= link_to hat.name, hat_path(hat) %></h3>
          <% end %>
        <% end %>

    <% else %>
        <% if @user.id == session[:id] %>
        <h3>You don’t have any hats!</h3>
      <% else %>
        <h3>This person has no hats!</h3>
      <% end %>
    <% end %>
    </h2>
      </div>
    </div>
  </div>
</body>
